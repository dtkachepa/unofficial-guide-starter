[project]
name = "unofficial-guide-starter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.6.0",
    "groq==0.15.0",
    "python-dotenv==1.0.1",
    "sentence-transformers==3.4.1",
]
